% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-barplot.R
\name{insper_barplot}
\alias{insper_barplot}
\title{Create a Bar Plot with Insper Styling}
\usage{
insper_barplot(
  data,
  x,
  y,
  fill = NULL,
  position = "dodge",
  palette = "categorical",
  zero = TRUE,
  text = FALSE,
  text_size = 4,
  text_color = "black",
  stack_vjust = 0.5,
  label_formatter = scales::comma,
  ...
)
}
\arguments{
\item{data}{A data frame containing the data to plot}

\item{x}{Column name for x-axis}

\item{y}{Column name for y-axis}

\item{fill}{Fill aesthetic. Accepts either:
\itemize{
  \item A bare column name for variable mapping (e.g., \code{fill = gear})
  \item A quoted color string for static fill (e.g., \code{fill = "blue"})
  \item \code{NULL} (default) to use default Insper red
}
When mapping a variable, creates grouped or stacked bars based on \code{position}.}

\item{position}{Position adjustment for bars. Options: "dodge", "stack",
"fill", "identity". Default is "dodge"}

\item{palette}{Character. Color palette for variable mappings. Default is "categorical".}

\item{zero}{Logical. If TRUE, adds a horizontal line at y = 0. Default is TRUE}

\item{text}{Logical. If TRUE, adds value labels on bars. Default is FALSE}

\item{text_size}{Numeric. Size of text labels. Default is 4}

\item{text_color}{Character. Color of text labels. Default is "black".
For position = "fill", automatic contrast-based colors are used if not specified.}

\item{stack_vjust}{Numeric. Vertical adjustment for text labels in stacked/filled bars.
Range: 0 (bottom) to 1 (top). Default is 0.5 (center). Only applies when
position = "stack" or "fill"}

\item{label_formatter}{Function. Formatter for text labels. Default is scales::comma.
For position = "fill", percentages are automatically formatted if values are proportions}

\item{...}{Additional arguments passed to \code{ggplot2::geom_col()},
allowing custom aesthetics like width, alpha, etc.}
}
\value{
A ggplot2 object
}
\description{
This function creates a customized bar plot using ggplot2 with Insper's
visual identity. Supports grouped bars, text labels, and automatic orientation.
}
\details{
The function automatically detects bar orientation based on variable types:
\itemize{
  \item **Vertical bars**: When x is categorical and y is numeric (default)
  \item **Horizontal bars**: When x is numeric and y is categorical
}

Text labels and zero lines automatically adjust to the detected orientation.
}
\examples{
\dontshow{if (has_insper_fonts()) withAutoprint(\{ # examplesIf}
# Simple bar plot with default color
insper_barplot(mtcars, x = factor(cyl), y = mpg)

# With text labels showing values
insper_barplot(mtcars, x = factor(cyl), y = mpg, text = TRUE)

# Stacked bars with centered text labels
data <- data.frame(
  category = rep(c("A", "B", "C"), each = 2),
  group = rep(c("X", "Y"), 3),
  value = c(10, 15, 20, 25, 30, 35)
)
insper_barplot(data, x = category, y = value, fill = group,
               position = "stack", text = TRUE)

# Filled bars with automatic percentage formatting
insper_barplot(data, x = category, y = value, fill = group,
               position = "fill", text = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{theme_insper}}, \code{\link{scale_fill_insper_d}}

Other plots: 
\code{\link{insper_area}()},
\code{\link{insper_boxplot}()},
\code{\link{insper_density}()},
\code{\link{insper_heatmap}()},
\code{\link{insper_histogram}()},
\code{\link{insper_scatterplot}()},
\code{\link{insper_timeseries}()},
\code{\link{insper_violin}()}
}
\concept{plots}
