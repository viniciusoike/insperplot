% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{insper_histogram}
\alias{insper_histogram}
\title{Insper Histogram}
\usage{
insper_histogram(
  data,
  x,
  fill = NULL,
  bins = NULL,
  bin_method = c("sturges", "fd", "scott", "manual"),
  fill_color = show_insper_colors("reds1"),
  border_color = "white",
  zero = TRUE
)
}
\arguments{
\item{data}{A data frame containing the data to plot}

\item{x}{<[`data-masked`][ggplot2::aes_eval]> Variable for x-axis (numeric)}

\item{fill}{<[`data-masked`][ggplot2::aes_eval]> Variable for fill aesthetic (optional)}

\item{bins}{Numeric. Number of bins. Only used when bin_method = "manual"}

\item{bin_method}{Character. Bin selection method: "sturges", "fd" (Freedman-Diaconis),
"scott", or "manual". Default is "sturges"}

\item{fill_color}{Character. Hex color for bars when not using fill aesthetic.
Default is Insper red.}

\item{border_color}{Character. Color for bar borders. Default is "white"}

\item{zero}{Logical. If TRUE, adds a horizontal line at y = 0. Default is TRUE}
}
\value{
A ggplot2 object
}
\description{
Create histograms with formal bin selection methods using Insper's visual identity.
Implements Sturges, Freedman-Diaconis, and Scott algorithms for optimal bin width.
}
\details{
Bin selection methods:
\itemize{
  \item **Sturges**: \eqn{k = \lceil \log_2(n) + 1 \rceil}. Works well for normal distributions.
  \item **Freedman-Diaconis**: Uses IQR to determine bin width. Robust to outliers.
  \item **Scott**: Uses standard deviation. Optimal for normal distributions.
  \item **Manual**: Specify exact number of bins with the `bins` parameter.
}
}
\examples{
\dontrun{
# Simple histogram with Sturges method
insper_histogram(mtcars, x = mpg)

# Using Freedman-Diaconis method
insper_histogram(mtcars, x = mpg, bin_method = "fd")

# Manual bin specification
insper_histogram(mtcars, x = mpg, bin_method = "manual", bins = 15)

# Grouped histogram
insper_histogram(mtcars, x = mpg, fill = factor(cyl))
}

}
\seealso{
\code{\link{theme_insper}}, \code{\link{insper_density}}

Other plots: 
\code{\link{insper_area}()},
\code{\link{insper_barplot}()},
\code{\link{insper_boxplot}()},
\code{\link{insper_density}()},
\code{\link{insper_heatmap}()},
\code{\link{insper_lollipop}()},
\code{\link{insper_scatterplot}()},
\code{\link{insper_timeseries}()},
\code{\link{insper_violin}()}
}
\concept{plots}
