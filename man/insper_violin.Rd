% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-violin.R
\name{insper_violin}
\alias{insper_violin}
\title{Insper Violin Plot}
\usage{
insper_violin(
  data,
  x,
  y,
  fill = NULL,
  palette = "categorical",
  show_boxplot = FALSE,
  show_points = FALSE,
  violin_alpha = 0.7,
  ...
)
}
\arguments{
\item{data}{A data frame containing the data to plot}

\item{x}{Variable for x-axis (categorical)}

\item{y}{Variable for y-axis (numeric)}

\item{fill}{Fill aesthetic. Accepts either:
\itemize{
  \item A bare column name for variable mapping (e.g., \code{fill = Species})
  \item A quoted color string for static fill (e.g., \code{fill = "purple"})
  \item \code{NULL} (default) to use default Insper teal
}}

\item{palette}{Character. Color palette for variable mappings. Default is "categorical".}

\item{show_boxplot}{Logical. If TRUE, overlays a boxplot. Default is FALSE}

\item{show_points}{Logical. If TRUE, adds jittered points. Default is FALSE}

\item{violin_alpha}{Numeric. Transparency of violins (0-1). Default is 0.7}

\item{...}{Additional arguments passed to \code{ggplot2::geom_violin()}}
}
\value{
A ggplot2 object
}
\description{
Create violin plots to visualize distributions using Insper's visual identity.
Optionally overlay boxplots and/or jittered points.
}
\examples{
\dontshow{if (has_insper_fonts()) withAutoprint(\{ # examplesIf}
# Simple violin plot with default fill
insper_violin(iris, x = Species, y = Sepal.Length)

# Static fill color
insper_violin(iris, x = Species, y = Sepal.Length, fill = "purple")

# Variable fill mapping
insper_violin(iris, x = Species, y = Sepal.Length, fill = Species)

# Custom palette
insper_violin(iris, x = Species, y = Sepal.Length, fill = Species, palette = "bright")

# With boxplot overlay and points
insper_violin(iris, x = Species, y = Sepal.Length,
              show_boxplot = TRUE, show_points = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{theme_insper}}, \code{\link{scale_fill_insper_d}}, \code{\link{insper_boxplot}}

Other plots: 
\code{\link{insper_area}()},
\code{\link{insper_barplot}()},
\code{\link{insper_boxplot}()},
\code{\link{insper_density}()},
\code{\link{insper_heatmap}()},
\code{\link{insper_histogram}()},
\code{\link{insper_scatterplot}()},
\code{\link{insper_timeseries}()}
}
\concept{plots}
