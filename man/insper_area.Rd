% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{insper_area}
\alias{insper_area}
\title{Insper Area Plot}
\usage{
insper_area(
  data,
  x,
  y,
  fill = NULL,
  stacked = FALSE,
  area_alpha = 1,
  add_line = TRUE
)
}
\arguments{
\item{data}{A data frame containing the data to plot}

\item{x}{<[`data-masked`][ggplot2::aes_eval]> Time variable (numeric, Date, or POSIXct)}

\item{y}{<[`data-masked`][ggplot2::aes_eval]> Value variable}

\item{fill}{<[`data-masked`][ggplot2::aes_eval]> Variable for fill aesthetic (optional)}

\item{stacked}{Logical. If TRUE and fill is provided, creates stacked areas.
Default is FALSE}

\item{area_alpha}{Numeric. Transparency of areas (0-1). Default is 0.6}

\item{add_line}{Logical. If TRUE, adds line on top of area. Default is TRUE}
}
\value{
A ggplot2 object
}
\description{
Create area charts for time series data using Insper's visual identity.
Supports both single and grouped/stacked areas.
}
\examples{
\dontrun{
# Simple area plot
df <- data.frame(time = 1:50, value = cumsum(rnorm(50)))
insper_area(df, x = time, y = value)

# Grouped areas
df <- data.frame(
  time = rep(1:50, 3),
  value = c(cumsum(rnorm(50)), cumsum(rnorm(50)), cumsum(rnorm(50))),
  group = rep(c("A", "B", "C"), each = 50)
)
insper_area(df, x = time, y = value, fill = group)

# Stacked areas
insper_area(df, x = time, y = value, fill = group, stacked = TRUE)
}

}
\seealso{
\code{\link{theme_insper}}, \code{\link{scale_fill_insper}}, \code{\link{insper_timeseries}}

Other plots: 
\code{\link{insper_barplot}()},
\code{\link{insper_boxplot}()},
\code{\link{insper_heatmap}()},
\code{\link{insper_lollipop}()},
\code{\link{insper_scatterplot}()},
\code{\link{insper_timeseries}()},
\code{\link{insper_violin}()}
}
\concept{plots}
